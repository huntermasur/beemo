# tests/

Test helpers, mocks, fixtures, and cross-cutting integration tests.

Shared testing infrastructure lives here. Unit tests can sit next to the code they cover
(`*.test.ts`); this folder is for the setup and the tests that span multiple sections.

Examples: `fixtures/`, `mocks/`, `helpers/`, integration/e2e suites.
