# {{projectName}}

All agent instructions for this repository live in [AGENTS.md](AGENTS.md) — read that
file first, then follow its required-reading list (`ai/` and `docs/`).

Claude-specific notes:

- MCP servers are configured in [.mcp.json](.mcp.json).
- Follow the documentation policy in AGENTS.md: notable changes are not complete
  until the affected docs (see docs/DOCUMENTATION_GUIDE.md) are updated.
